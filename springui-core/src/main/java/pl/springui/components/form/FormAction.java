package pl.springui.components.form;

/**
 * Typical actions that could be send from the frontend as a request parameter
 * 
 * @author dsu
 *
 */
public enum FormAction {
  add, cancel, delete, read, save,
}